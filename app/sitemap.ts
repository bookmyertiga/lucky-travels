import type {MetadataRoute} from "next";import {SITE,services} from "@/constants/site";import {blogPosts} from "@/data/blog";
export default function sitemap():MetadataRoute.Sitemap{const now=new Date();return[{url:SITE.url,lastModified:now,changeFrequency:"weekly",priority:1},{url:`${SITE.url}/about`,lastModified:now,changeFrequency:"monthly",priority:.7},{url:`${SITE.url}/contact`,lastModified:now,changeFrequency:"monthly",priority:.7},{url:`${SITE.url}/faq`,lastModified:now,changeFrequency:"monthly",priority:.6},{url:`${SITE.url}/blog`,lastModified:now,changeFrequency:"weekly",priority:.7},...services.map(s=>({url:`${SITE.url}/services/${s.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.8})),...blogPosts.map(p=>({url:`${SITE.url}/blog/${p.slug}`,lastModified:new Date(p.dateModified ?? p.date),changeFrequency:"monthly" as const,priority:.65}))]}
