import type {Metadata} from "next";import Image from "next/image";import {notFound} from "next/navigation";import Navbar from "@/components/layout/Navbar";import Footer from "@/components/layout/Footer";import FloatingWhatsApp from "@/components/shared/FloatingWhatsApp";import {blogPosts} from "@/data/blog";import {SITE} from "@/constants/site";
export function generateStaticParams(){return blogPosts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=blogPosts.find(x=>x.slug===slug);if(!p)return{};return{title:p.title,description:p.excerpt,alternates:{canonical:`/blog/${p.slug}`},openGraph:{title:p.title,description:p.excerpt,url:`${SITE.url}/blog/${p.slug}`,images:[p.image],type:"article"}}}
export default async function BlogPost({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=blogPosts.find(x=>x.slug===slug);if(!p)notFound();return <><Navbar/><article className="page-shell max-w-4xl py-10"><p className="text-xs font-black text-purple-700">GO BENGALURU BLOG</p><h1 className="mt-3 text-4xl font-black leading-tight sm:text-5xl">{p.title}</h1><p className="mt-3 text-sm text-slate-500">{new Date(p.date).toLocaleDateString("en-IN",{day:"numeric",month:"long",year:"numeric"})}</p><div className="relative mt-7 h-[300px] overflow-hidden rounded-2xl sm:h-[440px]"><Image src={p.image} alt={p.title} fill priority className="object-cover"/></div><div className="mx-auto mt-8 max-w-3xl space-y-5 text-lg leading-8 text-slate-700">{p.content.map(x=><p key={x}>{x}</p>)}</div></article><Footer/><FloatingWhatsApp/></>}
