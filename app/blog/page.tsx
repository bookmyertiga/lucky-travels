import type {Metadata} from "next";import Image from "next/image";import Link from "next/link";import Navbar from "@/components/layout/Navbar";import Footer from "@/components/layout/Footer";import FloatingWhatsApp from "@/components/shared/FloatingWhatsApp";import {blogPosts} from "@/data/blog";
export const metadata:Metadata={title:"Bengaluru Travel Blog",description:"Useful guides about Bengaluru airport transfers, Premium Ertiga rental and outstation travel."};
export default function Blog(){return <><Navbar/><main className="page-shell py-12"><p className="text-xs font-black text-amber-500">GO BENGALURU JOURNAL</p><h1 className="mt-2 text-4xl font-black">Travel guides and updates</h1><p className="mt-3 max-w-2xl text-slate-600">Add future posts by creating one entry in <code>data/blog.ts</code>. The blog listing and individual article pages are generated automatically.</p><div className="mt-8 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{blogPosts.map(p=><Link href={`/blog/${p.slug}`} key={p.slug} className="overflow-hidden rounded-xl border bg-white shadow-premium"><div className="relative h-52"><Image src={p.image} alt={p.title} fill className="object-cover"/></div><div className="p-5"><p className="text-xs font-bold text-purple-700">{new Date(p.date).toLocaleDateString("en-IN",{day:"numeric",month:"long",year:"numeric"})}</p><h2 className="mt-2 text-xl font-black">{p.title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{p.excerpt}</p></div></Link>)}</div></main><Footer/><FloatingWhatsApp/></>}
